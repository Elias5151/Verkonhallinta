# Student state

This directory stores per-student lab state that survives topology rebuilds.

Each student should have their own subdirectory, for example:

- student-state/student01/
- student-state/student02/

The deployment process will restore the selected student's persisted state automatically.
